# Reproducible multi-process H100 cluster FBPIC + SynchRad environment.
# Compile from this directory with:
#   uv pip compile requirements-cluster.in -o requirements-cluster.lock --python-version 3.12 --python-platform linux

-e .
-e ../synchrad
-e ../picnostics

cupy-cuda12x
numba==0.65.0
pyfftw
openpmd-api

mpi4py
